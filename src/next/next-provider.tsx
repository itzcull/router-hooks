'use client'

import React from 'react'
import { usePathname, useSearchParams, useRouter, useParams } from "next/navigation"

export function NextProvider({ children }: PropsWithChildren<>) {
  return
}
