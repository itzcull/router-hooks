'use client'

import React, { PropsWithChildren } from 'react'
import { usePathname, useSearchParams, useRouter, useParams } from "next/navigation"

export function NextProvider({ children }: PropsWithChildren<unknown>) {
  return
}
